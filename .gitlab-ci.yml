image: registry.cloud.fits/fits/builder:7-dev

stages:
    - test
    - push

test:
    stage: test
    script: make test
    except:
       - prod

push:
    stage: push
    script: docker-make --exclude-linting-rules rule3 --summary
    only:
      - master
      - prod
